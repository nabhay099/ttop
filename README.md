# ttop 📊
A lightweight, interactive system monitor for the terminal. Built with C++ and Ncurses.

## Installation

### Via Snap (Recommended)
sudo snap install ttop
# Note: For full hardware access, run:
sudo snap connect ttop:hardware-observe
sudo snap connect ttop:mount-observe